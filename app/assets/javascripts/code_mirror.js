	// $(document).ready(function() {
	    // $('#code').each(function() {
	         // var editor = CodeMirror.fromTextArea(this, {
	             // lineNumbers : true,
	             // matchBrackets : true
	         // });
	     // })
	// })